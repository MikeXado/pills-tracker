{
  "name": "pills-tracker",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "apps/pills-tracker/src",
  "projectType": "application",
  "targets": {
    "build": {
      "executor": "@nx/webpack:webpack",
      "outputs": [
        "{options.outputPath}"
      ],
      "defaultConfiguration": "production",
      "options": {
        "target": "node",
        "compiler": "tsc",
        "outputPath": "dist/apps/pills-tracker",
        "main": "apps/pills-tracker/src/main.ts",
        "tsConfig": "apps/pills-tracker/tsconfig.app.json",
        "assets": [
          "apps/pills-tracker/src/assets"
        ],
        "webpackConfig": "apps/pills-tracker/webpack.config.js"
      },
      "configurations": {
        "development": {},
        "production": {}
      }
    },
    "serve": {
      "executor": "@nx/js:node",
      "defaultConfiguration": "development",
      "options": {
        "buildTarget": "pills-tracker:build"
      },
      "configurations": {
        "development": {
          "buildTarget": "pills-tracker:build:development"
        },
        "production": {
          "buildTarget": "pills-tracker:build:production"
        }
      }
    },
    "lint": {
      "executor": "@nx/eslint:lint",
      "outputs": [
        "{options.outputFile}"
      ]
    },
    "test": {
      "executor": "@nx/jest:jest",
      "outputs": [
        "{workspaceRoot}/coverage/{projectRoot}"
      ],
      "options": {
        "jestConfig": "apps/pills-tracker/jest.config.ts"
      }
    }
  },
  "tags": []
}
